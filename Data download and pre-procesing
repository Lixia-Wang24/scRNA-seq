### Data Source
# scRNA-seq data was constructed using Single Cell 3' Kits v3.1. The SRA file was downloaded from GEO (accession number: GSM5292873); 
# Reference: 2021-Single-nucleus chromatin accessibility and transcriptomic characterization of Alzheimer’s disease

### pre-processing
# 1. convert the SRA file into a fastq file.

# 2. Install Cell Ranger

# 3. Run Cell Ranger
Genome_dir=./10x_reference
Transcriptome=refdata-gex-GRCh38-2024-A
scRNA_dir=./scRNAseq_fastq_files
cellranger count --id=sample_rna \  # Arbitrary name, used as the output file path
        --transcriptome=$Genome_dir/$Transcriptome \ # Path of the reference folder
        --fastqs=$scRNA_dir \
        --sample=RNA1 \ # Sample name (If multiple libraries are from the same source, use cellranger aggr to aggregate them)
        --chemistry=SC3Pv3 \ # Library preparation method: Single Cell 3 v3 (Default is automatic detection)
        --localcores=8 \ # CPU cores (Default is all cores)
        --localmem=28 \ # RAM (Default is 90% of available memory)
        --create-bam=true # Output BAM file
